// 
// pincode-field.spec.ts
// 
// created by Vadivel Babu
// this file contains unit testing for pincode component

import { PincodeField } from './pincode-field';

describe('pincode-field', () => {
  it('builds', () => {
    expect(new PincodeField()).toBeTruthy();
  });
});
