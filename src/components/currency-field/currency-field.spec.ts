// 
// currency-field.spec.ts
// 
// created by Vadivel Babu
// this file contains unit testing for currency field component

import { CurrencyField } from './currency-field';

describe('currency-field', () => {
  it('builds', () => {
    expect(new CurrencyField()).toBeTruthy();
  });
});
